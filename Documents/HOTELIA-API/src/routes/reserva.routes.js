const {Router} = require("express");
const rutasRese = Router();
const ctrRese =require("../controller/reserva.controller")

//rutasRese.get('/', ctrRese.obtener);
//rutasRese.get('/:id', ctrRese.obtenerId);
//rutasRese.get('/', ctrRese.obtener);
//rutasRese.put('/:id', ctrRese.edit);

module.exports = rutasRese;